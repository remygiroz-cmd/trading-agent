# Données marché
yfinance>=0.2.40
pandas>=2.0.0
numpy>=1.24.0

# HTTP : sert à la fois aux données, à Supabase (client REST PostgREST maison),
# aux 3 IA (DeepSeek/Grok/Claude) et à Telegram. Aucun SDK lourd.
requests>=2.31.0

# Chargement du .env en local (en prod, vraies variables d'environnement)
python-dotenv>=1.0.0

# NOTES :
# - Le SDK `supabase` n'est PAS utilisé (dépendance native pyiceberg incompatible
#   Python 3.14) -> client REST maison dans memory/database.py.
# - `python-telegram-bot` n'est PAS utilisé -> appels directs à l'API Bot via requests.
# - `ta-lib` n'est PAS utilisé -> indicateurs (RSI/MACD/MA) calculés en pandas pur.
# - Le fuseau horaire utilise zoneinfo (stdlib Python 3.9+), pas pytz.
